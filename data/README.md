# Data Directory

This directory contains runtime data files:
- `nova_photos.db` - SQLite database with photo metadata

**Note:** This folder is gitignored. The database is created automatically on first run.
